{% if entry.entryLocation|length %}
    {% redirect entry.entryLocation[0].url %}
{% elseif entry.webUrl|length %}
    {% redirect entry.webUrl %}
{% elseif entry.redirectUrl|length %}
    {% redirect entry.redirectUrl.url %}
{% else %}
    {% redirect '/start-here' %}
{% endif %}
